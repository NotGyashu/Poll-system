# Environment# Environment




























MIN_POLL_DURATION=10MAX_POLL_DURATION=300DEFAULT_POLL_DURATION=60# ===========================================# POLL CONFIGURATION# ===========================================# DB_PASSWORD=your_password# DB_USER=postgres# DB_NAME=poll_system# DB_PORT=5432# DB_HOST=localhost# (Only used if DATABASE_URL is not set)# Option 2: Use individual variables for local PostgreSQLDATABASE_URL=postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres# Get this from Supabase Dashboard → Settings → Database → Connection string → URI# Option 1: Use DATABASE_URL for Supabase/hosted databases (RECOMMENDED)# ===========================================# DATABASE CONFIGURATION# ===========================================FRONTEND_URL=http://localhost:3000# Frontend URL (for CORS)PORT=5000NODE_ENV=developmentNODE_ENV=development
PORT=5000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=poll_system
DB_USER=postgres
DB_PASSWORD=your_password_here

# Poll Configuration
DEFAULT_POLL_DURATION=60
MAX_POLL_DURATION=300
MIN_POLL_DURATION=10
